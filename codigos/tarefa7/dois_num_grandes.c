#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#define TAM_MAX 100

int main(){
    char x[TAM_MAX];
    char y[TAM_MAX];

    fgets(x , TAM_MAX , stdin);
    fgets(y , TAM_MAX , stdin);

    return 0;
}