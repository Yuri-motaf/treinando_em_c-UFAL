#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void permutacoes(int a, int i){
    if(i == a) return;

    long long int x; scanf("%lld", &x);

    fatorial()
    
}

int main() {
    int a; scanf("%d", &a);

    permutacoes(a, 1);
    return 0;
}
